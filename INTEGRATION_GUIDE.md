# 🔧 Guia de Integração - Funcionalidades do Mundial\n\n## 📝 Índice\n1. [Configuração Inicial](#configuração-inicial)\n2. [Integração com Supabase](#integração-com-supabase)\n3. [Integração com Frontend](#integração-com-frontend)\n4. [Fluxos de Dados](#fluxos-de-dados)\n5. [Testes](#testes)\n\n---\n\n## 🚀 Configuração Inicial\n\n### 1. Aplicar Migrações Supabase\n\n```bash\n# Conecta ao Supabase\nsupabase link\n\n# Aplica todas as migrações\nsupabase migration up\n\n# Verifica o status\nsupabase migration list\n```\n\n### 2. Seed de Dados (Opcional)\n\nPara testes, adicione dados iniciais:\n\n```sql\n-- Adicionar equipes do Mundial\nINSERT INTO public.world_cup_teams (country_code, team_name, flag_emoji, group_letter, coach_name, star_players)\nVALUES\n  ('BR', 'Brasil', '🇧🇷', 'A', 'Dorival Júnior', ARRAY['Neymar', 'Vinícius Jr', 'Rodrygo']),\n  ('AR', 'Argentina', '🇦🇷', 'A', 'Lionel Scaloni', ARRAY['Messi', 'Agüero', 'Martínez']),\n  ('FR', 'França', '🇫🇷', 'B', 'Didier Deschamps', ARRAY['Mbappé', 'Griezmann', 'Benzema']),\n  ('DE', 'Alemanha', '🇩🇪', 'B', 'Hansi Flick', ARRAY['Müller', 'Sané', 'Havertz']);\n\n-- Adicionar um jogo de exemplo\nINSERT INTO public.world_cup_matches \n(match_date, team_a_id, team_b_id, stage, status, stadium, city, country)\nSELECT \n  now() + interval '7 days',\n  (SELECT id FROM world_cup_teams WHERE country_code = 'BR'),\n  (SELECT id FROM world_cup_teams WHERE country_code = 'AR'),\n  'group',\n  'scheduled',\n  'Estádio Mané Garrincha',\n  'Brasília',\n  'Brasil';\n```\n\n---\n\n## 🔗 Integração com Supabase\n\n### 1. Configurar Cliente Supabase\n\nJá está configurado em `src/integrations/supabase/client.ts`:\n\n```typescript\nimport { createClient } from '@supabase/supabase-js'\n\nconst supabaseUrl = import.meta.env.VITE_SUPABASE_URL\nconst supabaseKey = import.meta.env.VITE_SUPABASE_ANON_KEY\n\nexport const supabase = createClient(supabaseUrl, supabaseKey)\n```\n\n### 2. Variáveis de Ambiente\n\nAdicione ao `.env.local`:\n\n```env\nVITE_SUPABASE_URL=https://your-project.supabase.co\nVITE_SUPABASE_ANON_KEY=your-anon-key\n```\n\n### 3. Configurar Edge Functions\n\n```bash\n# Deploy das funções\nsupabase functions deploy calculate-world-cup-points\nsupabase functions deploy award-engagement-points\n\n# Testar localmente\nsupabase functions serve\n```\n\n---\n\n## 🎨 Integração com Frontend\n\n### 1. Adicionar Contextos Necessários\n\nOs contextos já estão configurados em `App.tsx`:\n\n```typescript\n<RegionalThemeProvider>\n  <LanguageProvider>\n    <CurrencyProvider>\n      {/* Aplicação */}\n    </CurrencyProvider>\n  </LanguageProvider>\n</RegionalThemeProvider>\n```\n\n### 2. Usar Hooks de Contexto\n\nEm qualquer componente:\n\n```typescript\nimport { useAuth } from '@/contexts/AuthContext'\nimport { useLanguage } from '@/contexts/LanguageContext'\nimport { useCurrency } from '@/contexts/CurrencyContext'\n\nfunction MyComponent() {\n  const { user, role } = useAuth()\n  const { language, t } = useLanguage()\n  const { currency } = useCurrency()\n  \n  return <div>{t('key')}</div>\n}\n```\n\n### 3. Integrar Componentes de Navegação\n\nAdicione links nas barras de navegação:\n\n```typescript\n// Navbar.tsx\nconst navItems = [\n  { label: 'Mundial', href: '/mundial' },\n  { label: 'Bolão', href: '/bolao' },\n  { label: 'Fantasy', href: '/fantasy' },\n  { label: 'Pontos', href: '/pontos' },\n  { label: 'Fórum', href: '/forum-mundial' },\n]\n```\n\n---\n\n## 📊 Fluxos de Dados\n\n### Fluxo 1: Fazer uma Previsão\n\n```\n1. Utilizador acessa /bolao\n2. Carrega matches agendados\n3. Utilizador insere previsão\n4. Clica \"Salvar Previsões\"\n5. INSERT em world_cup_predictions\n6. Award 5 pontos (prediction_made)\n7. Sucesso!\n```\n\n**Código:**\n```typescript\nconst savePredictions = async () => {\n  // 1. Validar\n  if (!user) return\n  \n  // 2. Inserir previsões\n  const { error } = await supabase\n    .from('world_cup_predictions')\n    .insert(predictions)\n  \n  // 3. Award pontos\n  await fetch('/functions/v1/award-engagement-points', {\n    method: 'POST',\n    body: JSON.stringify({\n      userId: user.id,\n      regionId: userRegion.id,\n      reason: 'prediction_made'\n    })\n  })\n}\n```\n\n### Fluxo 2: Resultado de um Jogo\n\n```\n1. Admin atualiza resultado do jogo\n2. PATCH em world_cup_matches\n3. Chama Edge Function\n4. Calcula pontos para cada previsão\n5. Atualiza engagement_points\n6. Loga em engagement_points_log\n7. Notifica utilizadores\n```\n\n**Código:**\n```typescript\nconst updateMatchResult = async (matchId: string, teamAGoals: number, teamBGoals: number) => {\n  // 1. Atualizar match\n  const { error: updateError } = await supabase\n    .from('world_cup_matches')\n    .update({\n      team_a_goals: teamAGoals,\n      team_b_goals: teamBGoals,\n      status: 'completed'\n    })\n    .eq('id', matchId)\n  \n  // 2. Calcular pontos\n  const response = await fetch('/functions/v1/calculate-world-cup-points', {\n    method: 'POST',\n    body: JSON.stringify({\n      matchId,\n      teamAGoals,\n      teamBGoals\n    })\n  })\n}\n```\n\n### Fluxo 3: Criar Time de Fantasy\n\n```\n1. Utilizador clica \"Criar Time\"\n2. Preenche nome e descrição\n3. INSERT em fantasy_teams\n4. Redireciona para seleção de jogadores\n5. Utilizador seleciona 11 jogadores\n6. INSERT em fantasy_team_players (x11)\n7. Award 20 pontos (contest_entry)\n8. Sucesso!\n```\n\n---\n\n## ✅ Testes\n\n### Teste 1: Previsão Correta\n\n```bash\n# 1. Criar previsão\nPOST /rest/v1/world_cup_predictions\n{\n  \"user_id\": \"user-123\",\n  \"match_id\": \"match-456\",\n  \"region_id\": \"region-789\",\n  \"predicted_team_a_goals\": 2,\n  \"predicted_team_b_goals\": 1\n}\n\n# 2. Atualizar resultado do jogo\nPATCH /rest/v1/world_cup_matches/match-456\n{\n  \"team_a_goals\": 2,\n  \"team_b_goals\": 1,\n  \"status\": \"completed\"\n}\n\n# 3. Verificar pontos\nGET /rest/v1/engagement_points?user_id=eq.user-123&region_id=eq.region-789\n# Deve retornar: points = 25 (previsão correta)\n```\n\n### Teste 2: Ranking de Pontos\n\n```bash\n# Verificar leaderboard\nGET /rest/v1/engagement_points?order=points.desc&limit=10\n\n# Verificar histórico de um utilizador\nGET /rest/v1/engagement_points_log?user_id=eq.user-123&order=created_at.desc\n```\n\n### Teste 3: Fórum\n\n```bash\n# Criar tópico\nPOST /rest/v1/forum_topics\n{\n  \"title\": \"Discussão: Brasil vs Argentina\",\n  \"description\": \"Análise do jogo\",\n  \"category\": \"match_discussion\",\n  \"region_id\": \"region-789\",\n  \"created_by\": \"user-123\"\n}\n\n# Adicionar post\nPOST /rest/v1/forum_posts\n{\n  \"topic_id\": \"topic-456\",\n  \"user_id\": \"user-123\",\n  \"content\": \"Ótima análise!\"\n}\n```\n\n---\n\n## 🔐 Segurança\n\n### RLS Policies\n\nTodas as tabelas têm RLS ativado. Verificar:\n\n```sql\n-- Ver políticas de uma tabela\nSELECT * FROM pg_policies WHERE tablename = 'world_cup_predictions';\n```\n\n### Autenticação\n\nTodas as operações requerem `auth.uid()` válido:\n\n```typescript\n// Apenas utilizadores autenticados podem fazer previsões\nconst { data, error } = await supabase\n  .from('world_cup_predictions')\n  .insert({ /* ... */ })\n  // RLS garante que user_id = auth.uid()\n```\n\n---\n\n## 📱 Checklist de Implementação\n\n- [ ] Aplicar migrações Supabase\n- [ ] Configurar variáveis de ambiente\n- [ ] Deploy Edge Functions\n- [ ] Seed de dados iniciais (equipes, jogos)\n- [ ] Testar fluxo de previsões\n- [ ] Testar cálculo de pontos\n- [ ] Testar Fantasy Football\n- [ ] Testar Fórum\n- [ ] Testar Admin Dashboards\n- [ ] Testar RLS policies\n- [ ] Configurar notificações (opcional)\n- [ ] Deploy em produção\n\n---\n\n## 🐛 Troubleshooting\n\n### Problema: Previsões não salvam\n**Solução**: Verificar RLS policies e autenticação\n\n```sql\n-- Verificar se utilizador tem acesso\nSELECT * FROM world_cup_predictions WHERE user_id = 'seu-user-id';\n```\n\n### Problema: Pontos não são calculados\n**Solução**: Verificar Edge Function logs\n\n```bash\nsupabase functions logs calculate-world-cup-points\n```\n\n### Problema: Temas não aplicam\n**Solução**: Verificar RegionalThemeContext\n\n```typescript\n// Verificar se cores estão sendo injetadas no CSS\nconsole.log(document.documentElement.style.getPropertyValue('--color-primary'))\n```\n\n---\n\n## 📞 Suporte\n\nPara dúvidas:\n1. Consulta WORLD_CUP_FEATURES.md\n2. Verifica logs do Supabase\n3. Testa queries no SQL Editor do Supabase\n4. Contacta o time de desenvolvimento\n\n---\n\n**Versão**: 1.0  \n**Data**: Junho 2026  \n**Status**: Pronto para Produção ✅\n
